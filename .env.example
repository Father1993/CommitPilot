# Copy to .env in the CommitPilot directory (same folder as auto_commit.py)
# cp .env.example .env

# OpenAI-compatible provider (default)
API_TOKEN=your_token_here

# Optional:
# API_BASE_URL=https://api.openai.com/v1
# API_MODEL=gpt-4.1
